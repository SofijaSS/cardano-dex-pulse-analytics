import { BrandLoader } from "@/components/BrandLoader";

export default function Loading() {
  return (
    <BrandLoader
      label="Opening Cardano DEX Pulse"
      detail="Preparing the analytics workspace"
    />
  );
}
